'''
Created on Jul 15, 2015

@author: Daniel Bruce
'''

import copy

if __name__ == '__main__':
	pass

class BasicStringIntConverter(object):
	'''
	classdocs
	'''

	def __init__(self, pBase):
		'''
		Constructor
		'''
		self.base = pBase
		
	def toString(self, pBigInt):
		assert(pBigInt >= 0)
		lclStringList = []
		lclBigInt = copy.deepcopy(pBigInt)
		while pBigInt != 0:
			#print(pBigInt)
			lclStringList.append(chr(pBigInt % self.base))
			pBigInt -= pBigInt % self.base
			assert(int(pBigInt) == pBigInt)
			pBigInt = int(pBigInt / self.base)
		assert(pBigInt == 0)
		return ''.join(lclStringList)
		
	def toInt(self, pString):
		total = 0
		print(pString)
		lclStringList = list(pString)
		for index, value in enumerate(lclStringList):
			#print((128**index), ord(value))
			total += ((self.base ** (index)) * ord(value))
		return total	