'''
Created on Jul 20, 2015

@author: Daniel Bruce
'''

class GenericLabelledKeyGenerator(object):
	'''
	classdocs
	'''

	def __init__(self, pKeyGen = SimpleKeyGenerator()):
		'''
		Constructor
		'''
		self.keyGen = pKeyGen
	
	def generate(self, pPasswordList, pResult, pName = BasicBigIntGenerator(64).generate()):
		lclKey = self.keyGen.generate(pPasswordList, pResult.password())
		return SimpleLabelledKey(str(pName),pPasswordList.getIdentifierList(), pResult.identifier(), "Basic", lclKey)	